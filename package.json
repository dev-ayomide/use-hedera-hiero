{
  "name": "use-hedera-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter use-hedera test",
    "lint": "pnpm -r lint",
    "postinstall": "pnpm --filter use-hedera build",
    "dev": "pnpm --filter demo dev",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
